services:
  - type: web
    name: test-email-verify
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: free
    region: frankfurt
    envVars:
      - key: NODE_ENV
        value: production
